🧪 QUICK TEST GUIDE - نظام تسليم GitHub

════════════════════════════════════════════════════════════════════════════

## اختبار الـ Team Submission

### 1. اختبر صفحة التسليم:
1. سجّل دخول كـ Team
2. اذهب إلى Dashboard
3. اضغط "Submit Project"
4. يجب أن تشوف صفحة بـ "Submit Project" (بدل "Upload Project")

### 2. اختبر Real-time Validation:
اختبر هذه الروابط:
```
✅ Valid:
   https://github.com/myteam/quantum-solver
   https://github.com/user/repo-name
   https://github.com/username/project/

❌ Invalid:
   http://github.com/user/repo  (أتش تي تي بي، ليس إيتش تي تي بي إس)
   https://gitlab.com/user/repo  (GitLab، مش GitHub)
   github.com/user/repo          (بدون https://)
   https://github.com/           (بدون owner/repo)
```

أثناء الكتابة:
- 🟢 أخضر = رابط صحيح ✅
- 🔴 أحمر = رابط خاطئ ❌

### 3. اختبر الـ Submission:
1. أدخل رابط صحيح
2. اضغط "Submit Project"
3. يجب أن:
   - تشوف "Submitting..." في الزر
   - ترجع للـ Dashboard بعد نجاح
   - ترى "Submitted" بدل "Pending"

### 4. اختبر الـ Update:
1. عُد لصفحة التسليم
2. أدخل رابط جديد
3. اضغط Submit
4. يجب أن يحدّث الرابط تلقائياً

════════════════════════════════════════════════════════════════════════════

## اختبر Judge Dashboard

### 1. تقييم فريق بـ GitHub Project:
1. سجّل دخول كـ Judge
2. اختر فريق للتقييم
3. في الـ Evaluation Page:
   - يجب أن تشوف زر "🔗 View Project on GitHub" (بدل 📥 Download)
   - اضغط الزر
   - يجب أن ينفتح الـ GitHub repo في تبويب جديد

### 2. اختبر Error Cases:
- فريق مع submitted_at = NULL:
  - الزر ما يظهر
- فريق بدون project submission:
  - الزر معطّل

════════════════════════════════════════════════════════════════════════════

## اختبر Admin Dashboard

### 1. عرض تفاصيل الفريق:
1. سجّل دخول كـ Admin
2. اذهب للـ Dashboard
3. ابحث عن فريق
4. اضغط "View Details"
5. في الصفحة:
   - يجب أن تشوف زر "🔗 View on GitHub"
   - اضغطه
   - يجب أن ينفتح الـ GitHub repo في تبويب جديد

### 2. اختبر أن الرابط صحيح:
- الرابط يجب أن يكون الرابط الذي أدخله الفريق
- يجب أن يفتح في تبويب جديد

════════════════════════════════════════════════════════════════════════════

## اختبر Database:

### تحقق من البيانات:
```sql
-- اتصل بـ Database
psql -U username -d database_name

-- اختبر الـ projects table
SELECT 
  p.id,
  p.team_id,
  p.github_url,
  p.submitted_at
FROM projects p
LIMIT 5;

-- يجب أن تشوف:
-- id | team_id | github_url                                 | submitted_at
-- 1  | 1       | https://github.com/myteam/quantum-solver   | 2026-01-30...

-- اختبر أن file_path و public_id اختفوا:
-- يجب ما تشوفهم في النتائج
```

════════════════════════════════════════════════════════════════════════════

## اختبر Error Messages:

### في صفحة التسليم:
```
❌ Invalid GitHub URL. Must start with https://github.com/
✅ Valid GitHub URL
```

### في الـ API:
```json
{
  "message": "Invalid GitHub URL. Must start with https://github.com/"
}
```

════════════════════════════════════════════════════════════════════════════

## اختبر عدم وجود Errors في Browser Console:

افتح Developer Tools (F12):
- جنّاب Console
- يجب ما تشوف أي أخطاء حمراء ❌
- يجب ما تشوف multer errors
- يجب ما تشوف cloudinary errors

════════════════════════════════════════════════════════════════════════════

## اختبر عدم وجود Errors في Server Log:

في الـ Terminal:
- يجب ما تشوف أي multer errors
- يجب ما تشوف أي cloudinary errors
- يجب أن تشوف "Project submitted successfully" messages

════════════════════════════════════════════════════════════════════════════

## ✅ SUCCESS CRITERIA:

✓ Team يقدر يسلّم GitHub URL
✓ Real-time validation يعمل
✓ Valid URLs تُقبل
✓ Invalid URLs تُرفض
✓ Judge يقدر يشوف الرابط ويفتحه
✓ Admin يقدر يشوف الرابط ويفتحه
✓ لا أخطاء في Console
✓ لا أخطاء في Server Log
✓ Database يحفظ الرابط صحيح
✓ Resubmission يحدّث الرابط

════════════════════════════════════════════════════════════════════════════

## 🐛 TROUBLESHOOTING:

### المشكلة: Real-time validation ما تعمل
الحل:
- تأكد أن upload.js محمّل (اشوف Network tab)
- تأكد أن element ID "github_url" موجود في HTML

### المشكلة: الرابط ما يُحفظ في Database
الحل:
- تأكد أن github_url column موجود
- تأكد أن migration تمت بنجاح
- شوف server logs للـ SQL errors

### المشكلة: Judge/Admin button ما يفتح الرابط
الحل:
- تأكد أن الرابط موجود في Database
- تأكد أن openProjectRepository() تُعرّف
- تأكد أن API endpoint يُرجع github_url

### المشكلة: Form submission ما يعمل
الحل:
- تأكد من validation (جرّب رابط صحيح)
- شوف Network tab للـ request
- شوف Browser console للـ errors
- شوف Server logs

════════════════════════════════════════════════════════════════════════════

## 📞 SUPPORT:

إذا حصلت مشكلة:
1. شوف Browser Developer Tools (F12)
2. شوف Server Terminal logs
3. اقرأ GITHUB_MIGRATION.md
4. تحقق من GITHUB_IMPLEMENTATION_CHECKLIST.txt

════════════════════════════════════════════════════════════════════════════

تم إنشاء: يناير 30, 2026
آخر تحديث: يناير 30, 2026
